# Examples

Planned for v0.2.

This folder will hold example `brain/` setups for different kinds of developers (for example: backend developer, mobile developer, student) so new users can start from a filled example instead of a blank template.

For v0.1, use the templates in the top-level [`brain/`](../brain) folder and fill in the placeholders.
